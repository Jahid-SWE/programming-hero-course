function sumOfNumbers(numbers){
    sum=0;
    for(number of numbers){
        console.log(number);
        sum=sum+number;
    }
    return sum;
}

const numbers= [50,51,10,21];
const num= sumOfNumbers(numbers);
console.log('The result of Sum is : ',sum);